{
  "testCaseId": "TC-SQL-026",
  "databaseTestCase": "TC-SQL-026",
  "layer": "API",
  "group": "Settlement Math Verification",
  "priority": "P0",
  "title": "Verify settlement descriptions are populated for auditability",
  "apiCoverage": "derived",
  "baseUrl": "http://localhost:4001",
  "method": "GET",
  "endpoint": "/api/v1/merchants/{id}/settlements",
  "requestBody": null,
  "expectedStatus": 200,
  "expectedResponseFile": "../Responses/TC-SQL-026_response.json",
  "expectedResponse": [
    {
      "id": "<database-derived>",
      "merchantId": "<database-derived>",
      "amount": "<database-derived>",
      "description": "<database-derived>"
    }
  ],
  "sqlValidationFile": "../../SQL/TC-SQL-026_Verify_settlement_descriptions_are_populated_for_auditability.sql",
  "sqlValidation": "SELECT *\nFROM dbo.Settlements\nWHERE description IS NULL OR LTRIM(RTRIM(description)) = '';",
  "sqlExpectedResult": "0 rows are returned.",
  "dbReconciliation": "TC-SQL-026: Reconcile the API response from /api/v1/merchants/{id}/settlements against the SQL validation for TC-SQL-026; the API result must satisfy this case's expected database rule: 0 rows are returned.",
  "notes": "Endpoint and response contract taken only from the supplied Swagger. Response values shown as <database-derived> or <JWT> must be replaced/validated against the live API and SQL result."
}
