{
  "testCaseId": "TC-SQL-034",
  "databaseTestCase": "TC-SQL-034",
  "layer": "API",
  "group": "Data Hygiene & Constraints",
  "priority": "P1",
  "title": "Check invalid merchant wallet balances",
  "apiCoverage": "derived",
  "baseUrl": "http://localhost:4001",
  "method": "GET",
  "endpoint": "/api/v1/merchants/{id}/balance",
  "requestBody": null,
  "expectedStatus": 200,
  "expectedResponseFile": "../Responses/TC-SQL-034_response.json",
  "expectedResponse": {
    "merchantId": "<id>",
    "walletBalance": "<database-derived>"
  },
  "sqlValidationFile": "../../SQL/TC-SQL-034_Check_invalid_merchant_wallet_balances.sql",
  "sqlValidation": "SELECT *\nFROM dbo.Merchants\nWHERE walletBalance IS NULL OR walletBalance < 0;",
  "sqlExpectedResult": "0 rows are returned unless negative balances are explicitly supported.",
  "dbReconciliation": "TC-SQL-034: Reconcile the API response from /api/v1/merchants/{id}/balance against the SQL validation for TC-SQL-034; the API result must satisfy this case's expected database rule: 0 rows are returned unless negative balances are explicitly supported.",
  "notes": "Endpoint and response contract taken only from the supplied Swagger. Response values shown as <database-derived> or <JWT> must be replaced/validated against the live API and SQL result."
}
