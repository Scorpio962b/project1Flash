{
  "testCaseId": "TC-SQL-011",
  "databaseTestCase": "TC-SQL-011",
  "layer": "API",
  "group": "Financial Reconciliation",
  "priority": "P0",
  "title": "Verify duplicate wallet transaction signatures are absent",
  "apiCoverage": "derived",
  "baseUrl": "http://localhost:4001",
  "method": "GET",
  "endpoint": "/transactions/history",
  "requestBody": null,
  "expectedStatus": 200,
  "expectedResponseFile": "../Responses/TC-SQL-011_response.json",
  "expectedResponse": [
    {
      "id": "<database-derived>",
      "merchantId": "<database-derived>",
      "amount": "<database-derived>",
      "transactionType": "<database-derived>",
      "serviceType": "<database-derived>",
      "status": "completed",
      "reference": "<database-derived>"
    }
  ],
  "sqlValidationFile": "../../SQL/TC-SQL-011_Verify_duplicate_wallet_transaction_signatures_are_absent.sql",
  "sqlValidation": "SELECT merchantId, amount, transactionType, serviceType, status, reference, COUNT(*) AS duplicateCount\nFROM dbo.WalletTransactions\nGROUP BY merchantId, amount, transactionType, serviceType, status, reference\nHAVING COUNT(*) > 1;",
  "sqlExpectedResult": "0 rows are returned unless duplicates are explicitly allowed by the business rules.",
  "dbReconciliation": "TC-SQL-011: Reconcile the API response from /transactions/history against the SQL validation for TC-SQL-011; the API result must satisfy this case's expected database rule: 0 rows are returned unless duplicates are explicitly allowed by the business rules.",
  "notes": "Endpoint and response contract taken only from the supplied Swagger. Response values shown as <database-derived> or <JWT> must be replaced/validated against the live API and SQL result."
}
