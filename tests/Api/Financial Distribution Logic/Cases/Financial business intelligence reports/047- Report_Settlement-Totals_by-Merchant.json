{
  "testCaseId": "TC-SQL-047",
  "databaseTestCase": "TC-SQL-047",
  "layer": "API",
  "group": "Business Intelligence Reports",
  "priority": "P2",
  "title": "Report settlement totals by merchant",
  "apiCoverage": "derived",
  "baseUrl": "http://localhost:4001",
  "method": "GET",
  "endpoint": "/api/v1/merchants/{id}/settlements",
  "requestBody": null,
  "expectedStatus": 200,
  "expectedResponseFile": "../Responses/TC-SQL-047_response.json",
  "expectedResponse": [
    {
      "id": "<database-derived>",
      "merchantId": "<database-derived>",
      "amount": "<database-derived>",
      "description": "<database-derived>"
    }
  ],
  "sqlValidationFile": "../../SQL/TC-SQL-047_Report_settlement_totals_by_merchant.sql",
  "sqlValidation": "SELECT\n    m.id,\n    m.merchantName,\n    ROUND(COALESCE(SUM(s.amount),0),2) AS settlementTotal\nFROM dbo.Merchants m\nLEFT JOIN dbo.Settlements s ON s.merchantId = m.id\nGROUP BY m.id, m.merchantName\nORDER BY settlementTotal DESC;",
  "sqlExpectedResult": "Merchants are ranked correctly by total settlement value.",
  "dbReconciliation": "TC-SQL-047: Reconcile the API response from /api/v1/merchants/{id}/settlements against the SQL validation for TC-SQL-047; the API result must satisfy this case's expected database rule: Merchants are ranked correctly by total settlement value.",
  "notes": "Endpoint and response contract taken only from the supplied Swagger. Response values shown as <database-derived> or <JWT> must be replaced/validated against the live API and SQL result."
}
