{
  "testCaseId": "API-FD-13",
  "databaseTestCase": "DB-TC-28",
  "layer": "API",
  "group": "Financial & Distribution Logic",
  "priority": "P0",
  "title": "API-FD-13",
  "apiCoverage": "direct",
  "baseUrl": "http://localhost:4001",
  "method": "GET",
  "endpoint": "/api/v1/merchants/{id}/settlements",
  "requestBody": null,
  "expectedStatus": 200,
  "expectedResponseFile": "../Responses/API-FD-13_response.json",
  "expectedResponse": [
    {
      "id": "<database-derived>",
      "merchantId": "<database-derived>",
      "amount": "<database-derived>",
      "description": "<database-derived>"
    }
  ],
  "sqlValidationFile": "../../SQL/DB-TC-28_API-FD-13_List_settlements.sql",
  "sqlValidation": "SELECT s.*,m.merchantName FROM dbo.Settlements s JOIN dbo.Merchants m ON m.id=s.merchantId ORDER BY s.id;",
  "sqlExpectedResult": "API settlement records match the database records.",
  "dbReconciliation": "API-FD-13: Reconcile the API response from /api/v1/merchants/{id}/settlements against the SQL validation for DB-TC-28; the API result must satisfy this case's expected database rule: API settlement records match the database records.",
  "notes": "Endpoint and response contract taken only from the supplied Swagger. Response values shown as <database-derived> or <JWT> must be replaced/validated against the live API and SQL result."
}
