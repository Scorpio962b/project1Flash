{
  "testCaseId": "TC-SQL-019",
  "databaseTestCase": "TC-SQL-019",
  "layer": "API",
  "group": "Settlement Math Verification",
  "priority": "P0",
  "title": "Verify no settlement has more than two decimal places",
  "apiCoverage": "derived",
  "baseUrl": "http://localhost:4001",
  "method": "GET",
  "endpoint": "/api/v1/merchants/{id}/settlements",
  "requestBody": null,
  "expectedStatus": 200,
  "expectedResponseFile": "../Responses/TC-SQL-019_response.json",
  "expectedResponse": [
    {
      "id": "<database-derived>",
      "merchantId": "<database-derived>",
      "amount": "<database-derived>",
      "description": "<database-derived>"
    }
  ],
  "sqlValidationFile": "../../SQL/TC-SQL-019_Verify_no_settlement_has_more_than_two_decimal_places.sql",
  "sqlValidation": "SELECT *\nFROM dbo.Settlements\nWHERE amount * 100 <> ROUND(amount * 100,0);",
  "sqlExpectedResult": "0 rows are returned.",
  "dbReconciliation": "TC-SQL-019: Reconcile the API response from /api/v1/merchants/{id}/settlements against the SQL validation for TC-SQL-019; the API result must satisfy this case's expected database rule: 0 rows are returned.",
  "notes": "Endpoint and response contract taken only from the supplied Swagger. Response values shown as <database-derived> or <JWT> must be replaced/validated against the live API and SQL result."
}
