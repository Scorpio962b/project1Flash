{
  "testCaseId": "TC-SQL-018",
  "status": 200,
  "body": [
    {
      "id": "<database-derived>",
      "merchantId": "<database-derived>",
      "amount": "<database-derived>",
      "description": "<database-derived>"
    }
  ],
  "reconciliationRule": "TC-SQL-018: Reconcile the API response from /api/v1/merchants/{id}/settlements against the SQL validation for TC-SQL-018; the API result must satisfy this case's expected database rule: Each total matches the expected settlement total for that merchant."
}
