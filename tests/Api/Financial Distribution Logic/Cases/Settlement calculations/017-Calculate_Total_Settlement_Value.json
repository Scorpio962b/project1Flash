{
  "testCaseId": "TC-SQL-017",
  "databaseTestCase": "TC-SQL-017",
  "layer": "API",
  "group": "Settlement Math Verification",
  "priority": "P0",
  "title": "Calculate total settlement value",
  "apiCoverage": "derived",
  "baseUrl": "http://localhost:4001",
  "method": "GET",
  "endpoint": "/api/v1/merchants/{id}/settlements",
  "requestBody": null,
  "expectedStatus": 200,
  "expectedResponseFile": "../Responses/TC-SQL-017_response.json",
  "expectedResponse": [
    {
      "id": "<database-derived>",
      "merchantId": "<database-derived>",
      "amount": "<database-derived>",
      "description": "<database-derived>"
    }
  ],
  "sqlValidationFile": "../../SQL/TC-SQL-017_Calculate_total_settlement_value.sql",
  "sqlValidation": "SELECT ROUND(COALESCE(SUM(amount),0),2) AS totalSettlementValue\nFROM dbo.Settlements;",
  "sqlExpectedResult": "The result equals the approved total settlement value for the test dataset.",
  "dbReconciliation": "TC-SQL-017: Reconcile the API response from /api/v1/merchants/{id}/settlements against the SQL validation for TC-SQL-017; the API result must satisfy this case's expected database rule: The result equals the approved total settlement value for the test dataset.",
  "notes": "Endpoint and response contract taken only from the supplied Swagger. Response values shown as <database-derived> or <JWT> must be replaced/validated against the live API and SQL result."
}
